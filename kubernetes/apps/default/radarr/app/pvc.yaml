apiVersion: v1
kind: PersistentVolume
metadata:
  name: radarr
  namespace: default
spec:
  storageClassName: manual
  capacity:
    storage: 100M
  accessModes:
    - ReadWriteMany
  nfs:
    server: ${NFS_IP}
    path: "/config/radarr"
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: radarr
  namespace: default
spec:
  storageClassName: manual
  volumeName: radarr-config
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 100M